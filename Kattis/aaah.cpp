#include <bits/stdc++.h>

using namespace std;

int main()
{


    string a, b;
    cin >> a >> b;

    if ((int) a.size() >= (int) b.size())
        puts("go");
    else
        puts("no");

    return 0;
}

